{% set id = random(1..9) + random() %}
{% if  settings.style == 'popup-search' %}
	<div class="pavo-popup-search">
		<a href="#" class="{{ settings.button }}" data-target="#popup-search{{id}}"><i class="fa fa-search"></i></a>
		<div id="popup-search{{id}}" class="popup-search-container">
			<a href="#" class="close"><i class="fa fa-times" aria-hidden="true"></i></a>
			<div class="searchbox {{ settings.extra_class }}">
				<div class="searchbox-inner">
					{{ settings.search }}
				</div>
			</div>
		</div>
	</div>
{% else %}
	<div class="searchbox {{ settings.extra_class }}">{{ settings.search }}</div>
{% endif %}